{
  "name": "crustaceans.sh",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npx @11ty/eleventy && mkdir -p _site/assets && cp -R assets/* _site/assets/",
    "dev": "npx @11ty/eleventy --serve",
    "start": "npm run dev",
    "check": "npm run build && test -f _site/index.html",
    "preview": "python3 -m http.server 4173 -d _site",
    "deploy": "npm run build && npx wrangler deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@11ty/eleventy": "^3.1.5"
  }
}
